# Lint only the shipped plugin source (src/). Everything below is either not
# bundled into the published plugin or is a standalone non-Obsidian context.

# Dependencies and build output
node_modules/
main.js
coverage/

# Tests run under jsdom + ts-jest, not Obsidian.
tests/

# Build tooling
esbuild.config.mjs
jest.config.cjs
